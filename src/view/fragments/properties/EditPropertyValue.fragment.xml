<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core">
 
   <Popover
		id="dialogEditPropertyValue"
		title="Edit Property Value"
		class="sapUiContentPadding"
		placement="Vertical"
		afterClose=".onAfterClosePropertyValuePopover"
		contentWidth="15em"
		initialFocus="inpPropertyValue">

		<content>
				<VBox>
					<Input
						id="inpPropertyValue"
						value="{properties>property_value}"
						submit=".onClosePropertyValuePopover"/>
					
				</VBox>
		</content>

		<footer>
			<OverflowToolbar>
				<ToolbarSpacer/>
				<Button
					text="Delete"
					icon="sap-icon://delete"
					type="Reject"
					press=".onDeletePropertyValue" />
			</OverflowToolbar>
		</footer>
		
	</Popover>
</core:FragmentDefinition>